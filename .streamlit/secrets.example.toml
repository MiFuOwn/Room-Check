# Copy this file to .streamlit/secrets.toml (git-ignored) and fill in real
# values. Never commit secrets.toml itself.

[firebase]
database_url = "https://roomcheek-default-rtdb.asia-southeast1.firebasedatabase.app"

[line]
channel_access_token = ""   # from LINE Developers Console
target_user_id = ""         # the LINE user/group id to push alerts to
